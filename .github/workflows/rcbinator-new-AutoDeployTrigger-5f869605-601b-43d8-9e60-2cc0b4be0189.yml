name: Trigger auto deployment for rcbinator-new

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/rcbinator-new-AutoDeployTrigger-5f869605-601b-43d8-9e60-2cc0b4be0189.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.RCBINATORNEW_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.RCBINATORNEW_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.RCBINATORNEW_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}01-docker/web-app
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: devopsorkshop.azurecr.io
          registryUsername: ${{ secrets.RCBINATORNEW_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.RCBINATORNEW_REGISTRY_PASSWORD }}
          containerAppName: rcbinator-new
          resourceGroup: just-for-hacks
          imageToBuild: devopsorkshop.azurecr.io/rcbinator-new:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


